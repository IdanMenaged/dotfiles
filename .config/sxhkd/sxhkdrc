super + q
	bspc node -k

super + {_, shift + }{1-9,0}
	bspc {desktop -f, node -d} '^{1-9,10}'

super + {t, b}
	{kitty, firefox}

super + a
	rofi -show drun

super + {Left, Up, Down, Right}
	bspc node -f {west,north,south,east}

ctrl + F12
	gm import -window root ~/Pictures/screenshot.jpg
